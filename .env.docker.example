# EchoB – Docker-Compose Umgebungsvariablen
# Kopiere diese Datei nach .env.docker und passe sie an.
# Wird von docker-compose.yml über env_file: .env.docker geladen.

# ── Postgres (lokale Dev-Datenbank) ───────────────────────────────────────
POSTGRES_DB=echob
POSTGRES_USER=echob_dev
POSTGRES_PASSWORD=pgadmin

# ── API ───────────────────────────────────────────────────────────────────
ENVIRONMENT=development
LOG_LEVEL=debug

# Im Docker-Netzwerk ist der Hostname der Service-Name ("postgres")
DATABASE_URL=postgresql+asyncpg://echob_dev:pgadmin@postgres:5432/echob

# CORS: JSON-Array (pydantic-settings v2 erwartet dieses Format für list[str])
# In docker-compose.yml bereits gesetzt – hier nur bei Bedarf überschreiben:
# CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","http://localhost:8080"]

SECRET_KEY=local-dev-secret-not-for-production

# Supabase – leer lassen für rein lokale Entwicklung
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI – optional für lokale Tests
ANTHROPIC_API_KEY=

# ── pgAdmin (optional, --profile tools) ───────────────────────────────────
PGADMIN_EMAIL=dev@echob.dev
PGADMIN_PASSWORD=pgadmin

# ── Worker ────────────────────────────────────────────────────────────────
# Erbt DATABASE_URL und ANTHROPIC_API_KEY von oben

# ── Betriebsalarme (infra/monitor) ─────────────────────────────────────
# Wohin Backup-Fehler, Plattenwarnungen und API-Ausfaelle gemeldet werden.
# Sinnvoll ist ein Postfach AUSSERHALB der eigenen Domain: Wenn Resend, die Domain oder
# der Server das Problem sind, kommt die Warnung trotzdem an.
# Ohne diesen Eintrag benachrichtigt niemand jemanden.
ALARM_TO_EMAIL=

# Optional: Absender der Alarmmails. Ohne Angabe wird LEAD_FROM_EMAIL genommen.
# ALARM_FROM_EMAIL=alarm@echo-b.de

# Optional: oeffentlicher age-Schluessel fuer die Backup-Verschluesselung. Ohne Angabe
# nimmt infra/backup/backup.sh den im Skript hinterlegten. Beim Schluesselwechsel hier
# eintragen, statt das Skript zu aendern.
# ECHOB_AGE_RECIPIENT=age1...
